# Copy to .env and paste your key: https://novita.ai/settings/key-management
NOVITA_API_KEY=
NOVITA_BASE_URL=https://api.novita.ai/openai

# Model ids are configuration, never constants — Novita rotates its catalog and
# the display names on the website are not API ids. If either default 404s at
# startup, list what is actually served:
#
#   uv run python -m dungeon_master.cli --models
#
# Avoid reasoning/thinking models for the NARRATOR. They emit their scratchpad
# as `reasoning_content` and can burn the whole token budget before producing a
# single word of prose. Prefer an instruct-tuned model. (Verified: v4-flash is a
# reasoning model and starves narration; game.log warns explicitly if this
# happens.)
NARRATOR_MODEL=inclusionai/ling-3.0-flash
EXTRACTOR_MODEL=meta-llama/llama-3.3-70b-instruct
